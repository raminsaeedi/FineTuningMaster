# Figure Provenance

| Figure | Source | Scope | Interpretation limit |
|---|---|---|---|
| `chart_agreement_decomposition` | `tables/format_tolerant_chart_accuracy.csv` | Seed 42; four models; methods A–D; three scoring layers | Agreement with the frozen project reference, not independent design quality |
| `text_level_seed_variability` | Same CSV | Seeds 42–44 for Qwen3-1.7B and OLMo only | Individual observed seeds; connecting lines are not confidence intervals |

Both figures use a colorblind-safe palette plus distinct marker/line encodings. PDF is the manuscript source; PNG is supplied for preview. No image is decorative, generated by an image model, or reused from an obsolete report.
