"""
data — Synthetic dataset package.

Provides the generate_dataset() function for creating dashboard design
brief + recommendation pairs.

Usage:
    from data.generator import generate_dataset, generate_example
    train, val, test = generate_dataset(num_train=80, num_val=10, num_test=10)
"""
