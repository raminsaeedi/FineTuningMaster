# Staging folder for raw v2 generation batches.
# Candidate items are written here first; frozen files are produced only after
# validation/freezing. Batch files are regenerable and need not be committed.
