# ============================================================
# requirements.txt – dashboard-design-finetuning
# Python 3.10 or 3.11 recommended
# ============================================================

# --- Core: HuggingFace Ecosystem ---
transformers>=4.40.0        # Model loading, tokenization, training
datasets>=2.18.0            # Dataset handling (HuggingFace format)
tokenizers>=0.19.0          # Fast tokenizers (used by transformers)
accelerate>=0.29.0          # Multi-GPU / mixed precision / device placement
peft>=0.10.0                # LoRA, QLoRA, and other PEFT methods
trl>=0.8.6                  # SFTTrainer for supervised fine-tuning

# --- Quantization (needed for QLoRA / 4-bit training) ---
bitsandbytes>=0.43.0        # 4-bit and 8-bit quantization
                            # Windows: see install note below

# --- Config & Data ---
pyyaml>=6.0                 # Read train_config.yaml
jsonlines>=4.0.0            # Read/write .jsonl files

# --- Evaluation ---
evaluate>=0.4.1             # HuggingFace evaluate library
rouge-score>=0.1.2          # ROUGE metric for text comparison

# --- Utilities ---
tqdm>=4.66.0                # Progress bars
numpy>=1.24.0               # Numerical operations
pandas>=2.0.0               # Data inspection and analysis
scikit-learn>=1.3.0         # TF-IDF retrieval for RAG (rag/build_index.py)

# --- Visualization (optional, for loss curves) ---
matplotlib>=3.7.0

# --- Jupyter (for running the Colab notebook locally) ---
jupyter>=1.0.0
ipywidgets>=8.0.0

# ============================================================
# INSTALLATION INSTRUCTIONS
# ============================================================
#
# Step 1 – Create virtual environment:
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   source .venv/bin/activate       (Linux/Mac)
#
# Step 2 – Install PyTorch (choose ONE):
#
#   CPU only (for testing, no training):
#   pip install torch
#
#   NVIDIA GPU with CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
#   NVIDIA GPU with CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#
# Step 3 – Install this file:
#   pip install -r requirements.txt
#
# Step 4 – bitsandbytes on Windows (if needed):
#   pip install bitsandbytes --prefer-binary \
#     --extra-index-url=https://jllllll.github.io/bitsandbytes-windows-whl
#
# Step 5 – Verify installation:
#   python scripts/01_check_environment.py
