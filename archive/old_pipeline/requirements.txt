# ============================================================
# Master Thesis Fine-Tuning - Requirements
# Python 3.10+ recommended
# ============================================================

# --- Core ML Libraries ---
torch>=2.1.0                    # PyTorch (install separately with CUDA if needed)
transformers>=4.40.0            # HuggingFace Transformers
datasets>=2.18.0                # HuggingFace Datasets
tokenizers>=0.19.0              # Fast tokenizers

# --- PEFT / LoRA (QLoRA, DoRA, RSLoRA, LoftQ) ---
peft>=0.10.0                    # LoRA, DoRA (use_dora=True), RSLoRA (use_rslora=True),
                                #   LoftQ (LoftQConfig), and more
trl>=0.8.6                      # SFTTrainer (qlora/dora/rslora/loftq),
                                #   ORPOTrainer (orpo experiment)

# --- Quantization (for QLoRA / LoftQ) ---
bitsandbytes>=0.43.0            # 4-bit / 8-bit quantization (Linux/Windows GPU only)
accelerate>=0.29.0              # Multi-GPU / mixed precision training

# --- GaLore (full-parameter gradient projection) ---
galore-torch>=1.0               # Full-parameter training via gradient projection (ICML 2024)
                                # Install: pip install galore-torch

# --- Data & Config ---
pyyaml>=6.0                     # YAML config file parsing
jsonlines>=4.0.0                # JSONL file handling

# --- Evaluation & Metrics ---
rouge-score>=0.1.2              # ROUGE metrics for text evaluation
nltk>=3.8.1                     # NLP utilities
evaluate>=0.4.1                 # HuggingFace Evaluate library
scikit-learn>=1.3.0             # Macro-F1 for chart-type classification metrics

# --- Utilities ---
tqdm>=4.66.0                    # Progress bars
numpy>=1.24.0                   # Numerical computing
pandas>=2.0.0                   # Data manipulation
matplotlib>=3.7.0               # Plotting (for loss curves)
seaborn>=0.12.0                 # Statistical visualization

# --- Jupyter (for Colab notebook locally) ---
jupyter>=1.0.0
ipywidgets>=8.0.0

# ============================================================
# INSTALLATION NOTES:
# ============================================================
#
# 1. Standard install (CPU only, for testing):
#    pip install -r requirements.txt
#
# 2. With CUDA 11.8 (NVIDIA GPU):
#    pip install torch --index-url https://download.pytorch.org/whl/cu118
#    pip install -r requirements.txt
#
# 3. With CUDA 12.1 (NVIDIA GPU):
#    pip install torch --index-url https://download.pytorch.org/whl/cu121
#    pip install -r requirements.txt
#
# 4. bitsandbytes on Windows:
#    pip install bitsandbytes --prefer-binary --extra-index-url=https://jllllll.github.io/bitsandbytes-windows-whl
#
# 5. Apple Silicon (M1/M2/M3) - no bitsandbytes support:
#    pip install torch torchvision torchaudio  (MPS backend)
#    Remove bitsandbytes from requirements, set load_in_4bit: false in config.yaml
#
# 6. GaLore experiment (when implementing algorithms/galore.py):
#    pip install galore-torch
#    Uncomment the galore-torch line above
