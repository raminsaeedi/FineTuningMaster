# @package _global_
# Experiment E04 — Method D (FT+RAG) with Qwen2.5-0.5B-Instruct.
# Requires a trained adapter (see E03) and the knowledge base (build_kb.py).
defaults:
  - override /model: qwen2_5_0_5b
  - override /method: ft_rag
  - override /training: qlora_default
  - override /data: dashboard_v1
  - override /eval: full

experiment_name: E04_qwen0_5b_ft_rag
seed: 42
