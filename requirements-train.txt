# Training environment for the GPU machine (the professor's setup).
# Installs both the base stack and the training-only stack in one go:
#
#     pip install -r requirements-train.txt
#
# IMPORTANT — torch + CUDA:
# Install a CUDA-enabled PyTorch build that matches the machine's CUDA version
# BEFORE installing the rest, e.g. (CUDA 12.4):
#     pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# Then run the command above. The pins below are the versions this project was
# developed and verified against.

# --- base (inference / evaluation) ---
torch==2.6.0
transformers==5.7.0
pydantic==2.11.4
hydra-core==1.3.2
omegaconf==2.3.0
numpy==2.2.6
scipy==1.16.1
scikit-learn==1.8.0
pandas==2.2.3
pyyaml==6.0.2
tqdm==4.67.1

# --- training only (PEFT / QLoRA) ---
peft==0.19.1
trl==1.3.0
bitsandbytes==0.49.2
accelerate==1.13.0
datasets==4.8.5
